{% extends 'base.html.twig' %}

{% block title %}Ariane Back-Office
{% endblock %}

{% block body %}
	<h2>Supply</h2>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ supply.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ supply.name }}</td>
			</tr>
			<tr>
				<th>Slug</th>
				<td>{{ supply.slug }}</td>
			</tr>
			<tr>
				<th>Date de création</th>
				<td>{{ supply.createdAt ? supply.createdAt|date('Y-m-d') : '' }}</td>
			</tr>

		</tbody>
	</table>

	<a href="{{ path('app_back_supply_index') }}">back to list</a>

	<a href="{{ path('app_back_supply_edit', {'id': supply.id}) }}">edit</a>

	{{ include('back/supply/_delete_form.html.twig') }}

{% endblock %}
